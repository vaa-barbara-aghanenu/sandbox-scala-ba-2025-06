# Sandbox Scala BA 2025-06

This repository contains an empty Scala sandbox setup.

## Continuous Integration

A GitHub Actions workflow automatically checks code formatting and executes the test suite on every push or pull request targeting the `main` branch. The workflow ensures that all code is properly formatted and that tests pass before changes are merged.
